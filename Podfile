# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MoneyTap' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MoneyTap
    # Caching Images
    pod 'Kingfisher'
  target 'MoneyTapTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MoneyTapUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
